# Upstream IDA Pro MCP audit — 2026-09-23

Compared this fork with the official `mrexodia/ida-pro-mcp` repository at
[`fab3505`](https://github.com/mrexodia/ida-pro-mcp/commit/fab3505), from the
shared ancestor `cae2fa8`. The upstream side had 13 commits since that
ancestor, changing 14 files. This is a source audit, not a claim that the fork
has inherited upstream's release/support status.

| Upstream fix | Decision in this fork | Reason |
| --- | --- | --- |
| IDA Free uninstall no longer blocked (`f692c06`) | Adopted | Repairs the inherited installer without changing flow policy. |
| IPv6 authority retains brackets (`2c0424e`) | Adopted | Generated HTTP/SSE MCP URLs must remain parseable. |
| Blank/non-numeric `IDA_MCP_*` supervisor values (`e67d32c`) | Adopted, with an SDK-free subprocess regression test | Prevents Codex's forwarded empty environment values from stopping supervisor startup; `0` remains valid. |
| `entity_query`/`type_query` count `0` (`21d0b2f`) | Adopted | Matches the inherited tool schema's “0=all” contract. |
| String-inclusive JSON-RPC unions (`0e1c375`) | Adopted, with upstream regression tests | Prevents numeric-looking strings from becoming integers before validation. |
| Blank download base URL (`4460ea6`, `200fa16`) | Adopted, with upstream regression tests | Keeps truncated-output download hints absolute when `IDA_MCP_URL` is blank. |
| Sign-extended immediate matching (`df04acc`) | Adopted, with upstream IDA tests | Fixes high-bit x86 immediates and avoids reporting an address inside an instruction. |

The source changes were integrated as **one reviewed fork change**, not a
sequence of automated checkpoint/merge commits. The only textual conflict was
in `idalib_server.py`: the upstream blank-URL fallback was retained alongside
this fork's flow-runtime import. The upstream range made no README,
`pyproject.toml`, or Codex plugin-manifest changes, so those fork distribution
decisions are documented separately in [flow installation](flow-installation.md).

Validation boundary:

- SDK-free upstream/supervisor regressions and the existing flow suite are run
  under Python 3.11. Tests in `src/ida_pro_mcp/ida_mcp/tests/` require an IDA
  context and cannot be collected by ordinary SDK-free `pytest`; they are run
  through `ida-mcp-test` on a disposable `typed_fixture.elf` copy.
- Three static flow receipts name inherited implementation files whose hashes
  changed. They are regenerated by real IDA 9.3 runs, not by replacing hash
  fields. The target binaries are not executed.
- The upstream fixes do not expand this fork's reviewed `flow_*` support
  matrix, turn `partial` into a complete analysis, or add a vulnerability
  verdict. The release boundary remains in [flow compatibility](flow-compatibility.md).

## Tracking the next upstream change

The machine-readable [review marker](upstream-sync-state.json) pins
`fab3505` as the **last reviewed upstream commit** and `e349bc0` as its
integration commit in this fork. Because that integration was squashed,
GitHub's “13 behind” includes commits that were already reviewed; it must not
be used as the count of pending work.

Run from any directory with this checkout available:

```sh
python /path/to/ida-pro-mcp-taint/scripts/check_upstream_sync.py
# Optional machine-readable output:
python /path/to/ida-pro-mcp-taint/scripts/check_upstream_sync.py --json
```

The checker fetches only the official upstream `main`, verifies the recorded
commit is still its ancestor, and lists **only commits after that marker**. It
does not merge code or advance the marker. A rewritten upstream history or a
missing fork integration fails closed. The default exit code remains zero when
new commits are listed, so this manual check does not create a failing CI run;
`--fail-on-new` is available to callers that explicitly want exit code 2.
CI runs the checker's **offline tests**, not an unattended fetch or merge.

For a future sync, inspect each listed patch, integrate the selected upstream
change, run targeted and full tests plus affected IDA receipts, commit the
reviewed code, and only then update the marker to the new upstream SHA and
fork integration commit. Do not click GitHub's **Sync fork** merely to clear
the ancestry-based behind badge. No unattended upstream merge is enabled.
